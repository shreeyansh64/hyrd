# Hyrd

hyrd is a cross-platform job board built with Flutter and a modern, scalable backend stack.

The project uses Flutter for the client, Node.js for backend services, MongoDB for persistence, and follows a clean architecture powered by BLoC. Networking is handled with Retrofit, releases are managed with Shorebird, and the app supports internationalization using Flutter l10n.